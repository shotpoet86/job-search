/*import { evenOrOdd, multiply, total } from "@/components/playground";

describe("basic math", () => {
  it("add two numbers", () => {
    expect(1 + 1).toBe(2);
  });
  it("should subtract two numbers", () => {
    expect(5 - 2).toBe(3);
  });

  describe("even or odd", () => {
    describe("when the number is even", () => {
      it("indicates number is even", () => {
        expect(evenOrOdd(4)).toBe("Even");
      });
    });
    describe("when number is odd", () => {
      it("should indicate number is odd", function () {
        expect(evenOrOdd(3)).toBe("Odd");
      });
    });
  });
  describe("multiply two numbers", () => {
    it("should multiply the two numbers", () => {
      expect(multiply(2, 3)).toBe(6);
    });
  });

  describe("map through an array", () => {
    it("should return an instance of array", function () {
      expect(total).toStrictEqual([4, 6, 8, 10, 12, 14]);
    });
  });
});*/
